from fastapi import FastAPI, Request, HTException
from startup db_import user_credits
rates = 10 # Division rate of units per credit

app = FastAPI()

# Route to check user credits
}
@app.get("/check-credits")
def check_credits(user: str): 
    remaining credits=get(str)- 1 return `Remaining rates user:`}